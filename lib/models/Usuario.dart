class Usuario {
  int id;
  String email;
  String username;
  String password;

  Usuario(id, email, username, password) {
    this.id = id;
    this.email = email;

    this.password = password;
  }
}
